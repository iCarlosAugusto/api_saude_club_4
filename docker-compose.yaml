version: '3.7'

services:
  api:
    container_name: api
    restart: always
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - 3000:3000
    environment:
      - DATABASE_URL=file:./dev.db

    volumes: 
      - .:/usr/app